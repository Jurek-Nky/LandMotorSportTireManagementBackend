spring:
  config:
    import: private.yml
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
  datasource:
    url: jdbc:mysql://limla.ml:3306/tiremanagementTest?createDatabaseIfNotExist=true&serverTimezone=UTC
    driver:-class-name: com.mysql.cj.jdbc.Driver
server:
  error:
    include-message: always
  port: 8080